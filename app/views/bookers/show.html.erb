<% if flash[:notice] %>
<div class="flash">
	<%= flash[:notice] %>
</div>
<% end %>
<p>Title:<%= @book.title %></p>
<p>Body:<%= @book.body %></p>
<%= link_to "Edit", edit_booker_path(@book.id) %>
 | 
<%= link_to "Back", bookers_path %>